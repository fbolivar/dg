// Constantes de autenticación seguras para cliente y servidor (sin dependencias
// server-only). auth.ts las reexporta para mantener una sola fuente de verdad.

// Longitud mínima de contraseña (legal-tech: cuentas con acceso a datos sensibles).
export const MIN_PASSWORD_LENGTH = 10
